@model HelpOn.ViewModel.LaboratorioViewModel

@HelperMensagens.Message(Model.Mensagem, "alert bg-success", "glyph stroked checkmark", "#stroked-checkmark")

<br>

<div class="panel panel-default">
    <div class="panel-heading">
        <i class="glyphicon glyphicon-map-marker"></i>Laboratórios cadastrados
    </div>
    <div class="panel-body">

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Unidade</th>
                    <th>Andar</th>
                    <th>Laboratório</th>
                    <th>Remover</th>
                </tr>
            </thead>
            <tbody>
                @Html.Partial("_tabelaLaboratorios", Model.Laboratorios)
            </tbody>
        </table>
    </div>
</div>

<!-- Botão que redirecionar para o modal de cadastro de laboratório -->
<div class="pull-right">
    <button class="btn btn-primary btn-circle" data-target="#cadastrarLab" data-toggle="modal" type="button"><i class="glyphicon glyphicon-plus"></i></button>
</div>

<!-- Modal Cadastrar Laboratório -->
<div class="modal fade" id="cadastrarLab" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h3 align="center" class="modal-title" id="myModalLabel">Cadastro de Laboratório</h3>
            </div>
            @using (Html.BeginForm("CadastroLabs", "Unidade", FormMethod.Post))
            {
                <div class="modal-body">

                    <div class="form-group">
                        @Html.LabelFor(a => a.IDUnidade)
                        @Html.DropDownListFor(a => a.IDUnidade, Model.ListaUnidade, new { @class = "form-control" })
                        @Html.ValidationMessageFor(a => a.IDUnidade)
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(a => a.NumeroAndar)
                        @Html.DropDownListFor(a => a.NumeroAndar, Model.ListaAndar, new { @class = "form-control" })
                        @Html.ValidationMessageFor(a => a.NumeroAndar)
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(a => a.NumeroLab)
                        @Html.TextBoxFor(a => a.NumeroLab, new { @class = "form-control" })
                        @Html.ValidationMessageFor(a => a.NumeroLab)
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(a => a.IPMaquinaProf)
                        @Html.TextBoxFor(a => a.IPMaquinaProf, new { @class = "form-control" })
                        @Html.ValidationMessageFor(a => a.IPMaquinaProf)
                    </div>

                </div>
                <div class="modal-footer">
                    <button class="btn btn-primary" type="submit">Cadastrar</button>
                    <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
                </div>
            }
        </div>
    </div>
</div>

